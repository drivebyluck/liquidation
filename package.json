{
  "name": "solana-liquidations-proxy",
  "version": "1.0.0",
  "description": "Proxy server to fetch Solana liquidation data from CoinGlass",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
